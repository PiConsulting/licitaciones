# REGLAS DE LA CATEGORÍA "{root_key}"

Estas son las reglas exactas que el analista usó para producir la lista de
candidatos de abajo. Son las mismas reglas que vos tenés que aplicar --
no hay ninguna regla adicional.

{category_rules}

---

# CANDIDATOS A AUDITAR

Cada candidato tiene un `id` (0-indexado), y los campos que el analista
extrajo. `citas` son fragmentos literales del pliego que respaldan el ítem
(pueden estar recortadas).

```json
{candidate_items}
```

---

Revisá cada candidato contra las reglas de arriba y devolvé tu JSON de
decisiones.
